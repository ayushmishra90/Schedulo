# service_app/apps.py
from django.apps import AppConfig

class ServiceAppConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'service_app'  # Ensure this is correct
